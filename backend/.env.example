# Local dev (default) — SQLite, no extra setup
DATABASE_URL=sqlite:///./educompiler.db

# Production — uncomment and configure PostgreSQL
# DATABASE_URL=postgresql://postgres:password@localhost:5432/educompiler

EDU_HOST=0.0.0.0
EDU_PORT=8000
